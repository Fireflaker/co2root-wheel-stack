# GitHub Push

Local export repo prepared at:

`E:\Co2Root\wheel-stack-export`

Current commit:

`9b05923 Initial wheel stack export`

## Suggested new repo

- Owner: `Fireflaker`
- Name: `co2root-wheel-stack`

## After creating the repo on GitHub

Run:

```powershell
cd E:\Co2Root\wheel-stack-export
git remote add origin https://github.com/Fireflaker/co2root-wheel-stack.git
git push -u origin main
```

## What is included

- Wheel runtime and GUI in `adapter_project/`
- Top-level launch/setup scripts
- Legacy bridge reference
- End-to-end setup notes

## What is excluded

- Logs, screenshots, caches, downloads
- Unrelated workspace projects
- Large binary artifacts